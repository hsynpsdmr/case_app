name: case_app
description: "Case App is a VOCO application."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_login: ^5.0.0
  flutter_riverpod: ^2.4.9
  http: ^1.2.0
  shared_preferences: ^2.2.2
  flutter_offline: ^3.0.1
  lottie: ^3.0.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/lottie/